name: Disabled - old website deployment

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Inform about active deployment
        run: echo "This workflow is disabled. The active deployment is handled by deploy.yml for the Next.js site."
